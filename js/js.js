
/*window.addEventListener('resize', function () {
    
    if(window.innerWidth <= '1024' || window.innerWidth >= '390'){
        console.log(window.innerWidth);
        document.querySelector("#home").classList.add('d-md-block');
        document.querySelector("#about").classList.add('d-md-block');
   }else{
        document.querySelector("#home").classList.remove("d-dm-block");
        document.querySelector("#about").classList.remove("d-dm-block");
    }
});
jQuery(document).ready(function($) {
    var alterClass = function() {
      var ww = document.body.clientWidth;
      if (ww >= 390) {
        $('#home').removeClass('d-md-block');
        $('#about').removeClass('d-md-block');
      } else if (ww <= 1000) {
        $('#home').addClass('d-md-block');
        $('#about').addClass('d-md-block');
      };
    };
    $(window).resize(function(){
      alterClass();
    });
    //Fire it when the page first loads:
    alterClass();
  });*/